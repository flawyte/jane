<entity name="Example1">
  <attributes>
    <attribute name="id" type="Integer" required="true" /> <!-- required (explicit) -->
    <attribute name="name" type="String" /> <!-- required by default (implicit) -->
    <attribute name="completed" type="Boolean" optional="true" default="false" /> <!-- optional (explicit) -->
    <attribute name="otherNames" type="String[]" default="[]" /> <!-- optional array (implicit) -->
  </attributes>
</entity>